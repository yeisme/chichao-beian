# Claude Entry Point

Follow `AGENTS.md` in this directory. Claude Code uses `.claude/skills/` as the active runtime skill home; generic agents use `.agents/skills/`. Both homes are generated from `.skills/profiles/targets/data/chichao-beian.txt`.
